# --- Datenbank (Railway PostgreSQL) ---
DATABASE_URL="postgresql://postgres:password@localhost:5432/leads?schema=public"

# --- Claude Code Routine ---
# Volle URL der Routine (API-Trigger)
CLAUDE_ROUTINE_URL=""
# Bearer Token für die Routine (NIEMALS im Frontend)
CLAUDE_ROUTINE_TOKEN=""

# --- Mail-Fallback (optional, via Resend) ---
RESEND_API_KEY=""
NOTIFICATION_EMAIL="vertrieb@inconcepts.at"
NOTIFICATION_FROM="leads@inconcepts.at"

# --- Admin-Seite (/admin, Basic Auth) ---
ADMIN_USER=""
ADMIN_PASS=""
